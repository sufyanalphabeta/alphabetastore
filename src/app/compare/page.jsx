import ComparePageView from "pages-sections/compare/page-view";

export const metadata = {
  title: "Compare Products — Alphabeta Store",
  description: "Compare product specifications, prices, and availability side by side."
};

export default function ComparePage() {
  return <ComparePageView />;
}
